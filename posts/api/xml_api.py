import logging

import requests


def get_post_by_id(params: dict) -> str:
    # # params = url.create_api_params(tags=f'id:{post_id}')
    #
    # logging.info(f'Fetching post with id {post_id} request for url {url}')
    #
    # resp = requests.get(url, params)
    # resp.raise_for_status()
    #
    # return resp.text
    return None


